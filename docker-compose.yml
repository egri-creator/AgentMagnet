version: "3.9"
services:
  agentmagnet:
    build: .
    container_name: agentmagnet
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
      - AM_HTTP_HOST=0.0.0.0
      - AM_HTTP_PORT=8000
    restart: unless-stopped
